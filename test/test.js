/*var server = require('../server.js');

describe('nameOfImage', function() {
  before(function() {
    getImage(800);
  });
    it('1.jpg if width 1000', function() {
      assert.equal(getImg, '1.jpeg');
    });
});
*/
